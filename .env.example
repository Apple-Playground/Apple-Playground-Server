# GitHub OAuth 설정
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=3306
DB_SCHEMA=appleplayground
DB_USERNAME=root
DB_PASSWORD=your_database_password_here

# Redis 설정 (선택사항)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here

# AWS S3 설정
AWS_S3_ACCESS_KEY=your_aws_access_key_here
AWS_S3_SECRET_KEY=your_aws_secret_key_here
AWS_S3_BUCKET_NAME=your_s3_bucket_name_here
AWS_S3_REGION=ap-northeast-2

# 사용법:
# 1. 이 파일을 .env로 복사하세요
# 2. 실제 값으로 변경하세요
# 3. source .env로 환경변수를 로드하세요

# AWS S3 버킷 설정 가이드:
# 1. AWS Console에서 S3 버킷 생성
# 2. IAM에서 S3 접근 권한을 가진 사용자 생성
# 3. 버킷 정책에서 공개 읽기 권한 설정 (선택사항)
# 4. CORS 설정 (필수)

# AWS S3 CORS 설정 예시:
# [
#   {
#     "AllowedHeaders": ["*"],
#     "AllowedMethods": ["GET", "PUT", "POST", "DELETE"],
#     "AllowedOrigins": ["http://localhost:3000", "https://yourdomain.com"],
#     "ExposeHeaders": ["ETag"]
#   }
# ]
